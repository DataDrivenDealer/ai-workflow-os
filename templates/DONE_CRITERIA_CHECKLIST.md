# Definition of Done (DoD) - Checklist Template

**Purpose**: 提供明确的、可量化的完成标准，确保任务真正达到"完成"状态，避免虚假完成信号。  
**Inspired By**: Leslie Lamport 的形式化验证方法论  
**Usage**: 将此清单复制到每个 TaskCard，根据任务类型调整具体指标。

---

## ✅ 核心完成标准（必须全部满足）

### 1. 代码质量 (Code Quality)

- [ ] **测试覆盖率 ≥ 80%**
  - 验证方法: `pytest --cov=<module> --cov-report=term-missing`
  - 覆盖率报告路径: `reports/coverage/<module>_coverage.html`
  - 例外情况: UI代码、配置文件可豁免，需在TaskCard说明

- [ ] **所有单元测试通过**
  - 验证方法: `pytest <module>/tests/ -v`
  - 无 SKIP 或 XFAIL（除非在TaskCard明确说明原因）
  - 测试运行时间 < 5分钟（单个模块）

- [ ] **静态代码分析通过**
  - Linting: `ruff check <module>` 无错误
  - Type Checking: `pyright <module>` 无 error（warning可接受）
  - 复杂度: McCabe complexity ≤ 10 (可在特定函数豁免)

- [ ] **代码审查通过 (Code Review)**
  - 至少1名团队成员 approve
  - 所有 review comments 已解决或在TaskCard记录为 defer
  - 无阻塞性 TODO 或 FIXME（非阻塞性TODO需标记issue号）

---

### 2. 文档完整性 (Documentation)

- [ ] **API文档完整**
  - 所有公开函数/类有 docstring (Google风格)
  - 复杂算法有实现说明 (algorithm notes)
  - 关键决策在 ADR (Architecture Decision Record) 中记录

- [ ] **使用示例可运行**
  - README.md 或 docs/ 中有使用示例
  - 示例代码可独立运行（无需额外依赖或配置）
  - 如有 Jupyter Notebook，所有 cell 可顺序执行

- [ ] **变更日志更新**
  - CHANGELOG.md 或 TaskCard 中记录主要变更
  - 破坏性变更（Breaking Changes）高亮标注
  - 迁移指南（如适用）

---

### 3. 集成验证 (Integration)

- [ ] **依赖关系验证**
  - 上游依赖已满足（前置TaskCard状态为 `merged` 或 `released`）
  - 下游接口契约不变（或已通知下游团队）
  - 第三方依赖版本锁定在 `requirements-lock.txt`

- [ ] **集成测试通过**
  - 与相关模块的集成测试通过
  - 端到端测试场景覆盖（如适用）
  - 性能基准未退化 (regression test)

- [ ] **Gate验证通过**
  - 对应的 Governance Gate (G1/G2/G3/G4) 已通过
  - Gate报告已生成并归档到 `reports/gates/`
  - 所有Gate检查项为 PASS 或 WAIVED（豁免需记录原因）

---

### 4. 部署就绪 (Deployment Readiness)

- [ ] **配置管理**
  - 配置文件已更新（如 `configs/*.yaml`）
  - 环境变量在 `.env.example` 中文档化
  - 敏感配置不硬编码（使用环境变量或密钥管理）

- [ ] **回归测试通过**
  - 运行完整测试套件: `pytest kernel/tests/ -v`
  - 所有现有功能无破坏（无 regression）
  - CI/CD pipeline 绿灯（如已配置）

- [ ] **回滚计划**
  - 明确回滚步骤（在TaskCard或运维文档）
  - 数据迁移有逆向脚本（如适用）
  - 已验证回滚不丢失数据

---

### 5. 状态同步 (State Synchronization)

- [ ] **任务状态更新**
  - `state/tasks.yaml` 状态为 `reviewing` 或 `merged`
  - 所有 events 时间戳真实（非simulation）
  - 工时记录与实际匹配（±20%容差）

- [ ] **代码提交完整**
  - 所有变更已提交到Git（`git status` clean）
  - Commit message 遵循规范: `<type>(<scope>): <description>`
  - Branch已push到远程（如需协作）

- [ ] **制品归档**
  - 测试报告归档: `reports/tests/<task_id>/`
  - 覆盖率报告归档: `reports/coverage/<task_id>/`
  - Gate报告归档: `reports/gates/<task_id>/`

---

## 🎓 任务类型特定标准

### A) 研究型任务 (Research Tasks)

- [ ] 研究结论文档化（Markdown或Jupyter Notebook）
- [ ] 关键假设和限制明确列出
- [ ] 可复现性: 随机种子锁定，依赖版本固定

### B) 数据任务 (Data Tasks)

- [ ] 数据质量验证通过（completeness, consistency, causality）
- [ ] 数据校验和 (checksum) 记录
- [ ] 数据加载示例代码可运行

### C) 架构任务 (Architecture Tasks)

- [ ] 架构图更新 (Mermaid/UML)
- [ ] 接口契约文档化 (YAML/OpenAPI)
- [ ] 依赖边界审计通过（无反向依赖）

### D) 运维任务 (Ops Tasks)

- [ ] 运维手册更新
- [ ] 监控指标配置（如适用）
- [ ] 告警阈值设定（如适用）

---

## 🚫 反模式警告 (Anti-Patterns)

**如果以下任一条件满足，任务不应标记为 DONE**:

1. ❌ "测试稍后补"（Tests will be added later）
2. ❌ "文档口头传达"（Documentation is tribal knowledge）
3. ❌ "本地测试通过但CI跳过"（Works on my machine）
4. ❌ "临时方案未记录"（Temporary workarounds undocumented）
5. ❌ "破坏性变更未通知"（Breaking changes not communicated）
6. ❌ "依赖未满足但先merge"（Dependencies unmet but merged anyway）

---

## 📊 验证清单汇总

| 类别 | 检查项数量 | 必须全过 | 可豁免 |
|------|-----------|---------|--------|
| 代码质量 | 4 | ✅ | 测试覆盖率（需说明） |
| 文档完整性 | 3 | ✅ | - |
| 集成验证 | 3 | ✅ | Gate检查（需waiver） |
| 部署就绪 | 3 | ✅ | 回滚计划（简单任务） |
| 状态同步 | 3 | ✅ | - |

**总计**: 16 个核心检查项

---

## 📝 使用示例

```yaml
# 在 TaskCard frontmatter 中引用
verification:
  dod_checklist: "templates/DONE_CRITERIA_CHECKLIST.md"
  completed_items: 16
  waived_items: 
    - "test_coverage: UI code exemption"
  gate_report: "reports/gates/TASK_001_G2_PASSED.md"
```

---

**Last Updated**: 2026-02-02  
**Maintained By**: Project Orchestrator  
**Review Cycle**: Quarterly
